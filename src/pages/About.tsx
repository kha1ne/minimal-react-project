import type { FC } from "react";

const About: FC = () => (
  <>
    <h1>About</h1>
    <p>A minimal React 19 + Lit + TypeScript + Vite boilerplate with hybrid web component support.</p>
  </>
);

export default About;
