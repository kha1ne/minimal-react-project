# Build-time variables (used by Vite config, not accessible in browser)
NODE_ENV=development
PORT=5173

# Runtime variables (accessible in browser via import.meta.env.VITE_*)
VITE_LOG_LEVEL=debug
VITE_LOG_TIMESTAMP=true
